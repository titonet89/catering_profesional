# 🏗️ Arquitectura del Sistema - Catering Profesional

## 📊 Diagrama General

```
┌─────────────────────────────────────────────────────────────┐
│                      USUARIO FINAL                           │
│            (Navegador - Cliente Web)                        │
└────────────┬────────────────────────────────────────────────┘
             │
             │ HTTP/HTTPS
             │
┌────────────▼─────────────────────────────────────────────────┐
│                      FRONTEND (PUBLIC)                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              HTML Structure                            │ │
│  │  • index.html (Sitio Principal)                        │ │
│  │  • Secciones: Hero, Servicios, Presupuesto, etc      │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │            CSS Styling & Animations                   │ │
│  │  • styles.css (1782 líneas)                           │ │
│  │  • admin.css (600 líneas)                             │ │
│  │  • 15+ animaciones CSS                                │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │          JavaScript Logic (Vanilla JS)                │ │
│  │  • auth.js (400 líneas) - Autenticación               │ │
│  │  • app.js - Lógica principal                          │ │
│  │  • presupuesto.js - Calculadora                       │ │
│  │  • carrusel.js - Carrusel de eventos                  │ │
│  │  • galeria.js - Galería con filtros                   │ │
│  │  • formularios.js - Validación de forms               │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ API Requests (JSON)
             │
┌────────────▼─────────────────────────────────────────────────┐
│                    BACKEND (SERVIDOR)                        │
│                   Node.js + Express.js                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Middleware Layer                          │ │
│  │  • CORS - Control de acceso                            │ │
│  │  • JSON Parser - Parseo de datos                       │ │
│  │  • JWT Verify - Verificación de tokens                │ │
│  │  • File Upload - Manejo de archivos                    │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │          API Endpoints & Controllers                   │ │
│  │                                                         │ │
│  │  Públicos (sin autenticación):                          │ │
│  │  • GET /api/config - Configuración del sitio           │ │
│  │  • GET /api/servicios - Lista de servicios             │ │
│  │  • GET /api/galeria - Galería de fotos                 │ │
│  │  • GET /api/comentarios - Testimonios                  │ │
│  │  • POST /api/contacto - Enviar solicitud               │ │
│  │  • POST /api/comentarios - Enviar testimonio           │ │
│  │  • POST /api/upload/* - Subir archivos                 │ │
│  │                                                         │ │
│  │  Autenticación:                                         │ │
│  │  • POST /api/auth/google - Google Sign-In              │ │
│  │  • POST /api/auth/facebook - Facebook Login            │ │
│  │  • POST /api/auth/email - Email/Password               │ │
│  │                                                         │ │
│  │  Protegidos (requieren JWT):                            │ │
│  │  • POST /api/auth/logout - Cerrar sesión               │ │
│  │  • GET /api/usuario/perfil - Datos del usuario         │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           Security & Authentication                    │ │
│  │  • JWT Tokens (7 días expiración)                      │ │
│  │  • Token Verification Middleware                       │ │
│  │  • Protected Routes                                    │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ (Opcional en producción)
             │
┌────────────▼─────────────────────────────────────────────────┐
│              DATA LAYER (Base de Datos)                      │
│  • MongoDB (Recomendado - No SQL)                            │
│  • PostgreSQL (Alternativa - SQL)                            │
│  • Almacenamiento de:                                        │
│    - Usuarios                                                │
│    - Servicios                                               │
│    - Contactos                                               │
│    - Comentarios                                             │
│    - Eventos                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Autenticación (Secuencia)

```
Usuario                Browser              Server              Google/FB
  │                       │                    │                    │
  ├─ Click Login ─────────▶│                    │                    │
  │                       │                    │                    │
  │                       │ Open Modal         │                    │
  │                       │◀────────────────────│                    │
  │                       │                    │                    │
  ├─ Selecciona Google ───▶│                    │                    │
  │                       │                    │                    │
  │                       │ Redirige a Google ─────────────────────▶│
  │                       │                    │                    │
  │  Selecciona Cuenta    │                    │                    │
  │◀──────────────────────────────────────────────────────────────│
  │                       │                    │                    │
  │                       │ Retorna Token     │                    │
  │                       │◀───────────────────────────────────────│
  │                       │                    │                    │
  │                       │ POST /api/auth/google (token)         │
  │                       │────────────────────▶│                    │
  │                       │                    │                    │
  │                       │ Verifica JWT       │                    │
  │                       │                    │                    │
  │                       │ Crea JWT Local     │                    │
  │                       │                    │                    │
  │                       │ Retorna Token      │                    │
  │                       │◀────────────────────│                    │
  │                       │                    │                    │
  │                       │ Guarda en Storage  │                    │
  │                       │ Actualiza UI       │                    │
  │                       │                    │                    │
  │ Ver Perfil en Navbar  │                    │                    │
  │◀──────────────────────│                    │                    │
  │                       │                    │                    │
```

---

## 📁 Estructura de Archivos

```
CateringProfesional/
│
├── server.js                           ← Backend principal (400 líneas)
├── .env                                ← Configuración de variables
├── package.json                        ← Dependencias
├── package-lock.json
│
├── public/                             ← Archivos públicos (Frontend)
│   ├── index.html                      ← Sitio principal
│   │
│   ├── assets/
│   │   ├── css/
│   │   │   ├── styles.css             ← Estilos principales (1782 líneas)
│   │   │   └── admin.css              ← Admin panel styling (600 líneas)
│   │   │
│   │   └── js/
│   │       ├── app.js                 ← App principal
│   │       ├── auth.js                ← Autenticación (400 líneas)
│   │       ├── presupuesto.js         ← Presupuesto (200 líneas)
│   │       ├── carrusel.js            ← Carrusel
│   │       ├── galeria.js             ← Galería
│   │       └── formularios.js         ← Formularios
│   │
│   ├── images/                         ← Carpeta de imágenes
│   ├── videos/                         ← Carpeta de videos
│   │
│   └── uploads/                        ← Archivos subidos
│       ├── fotos/
│       └── videos/
│
├── admin/                              ← Panel de administración
│   └── index.html                      ← UI del admin panel
│
├── Documentación/
│   ├── GUIA_AUTENTICACION_SOCIAL.md
│   ├── GUIA_AUTENTICACION_IMPLEMENTADA.md
│   ├── SETUP_AUTENTICACION.md
│   ├── REFERENCIA_RAPIDA_AUTH.md
│   ├── INSTRUCCIONES_DESPLIEGUE.md
│   ├── RESUMEN_PROYECTO_FINAL.md
│   ├── ARQUITECTURA_SISTEMA.md         ← Este archivo
│   └── README.md                       ← Overview general
│
└── node_modules/                       ← Dependencias (npm install)
```

---

## 🔗 Dependencias del Proyecto

```json
{
  "dependencies": {
    "express": "4.x",                   ← Framework web
    "cors": "latest",                   ← Control de acceso
    "express-fileupload": "latest",     ← Upload de archivos
    "jsonwebtoken": "9.x",              ← JWT tokens
    "dotenv": "latest"                  ← Variables de entorno
  },
  "devDependencies": {
    "nodemon": "latest"                 ← Auto-reload en desarrollo
  }
}
```

---

## 🔐 Flujo de Seguridad

```
┌─────────────────────────────────┐
│     Petición del Cliente         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│    CORS Middleware              │
│  ✓ Verificar origen             │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  JSON Parser Middleware         │
│  ✓ Parsear body                 │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│   Rutas Públicas OK             │
│   ✓ GET /api/config             │
│   ✓ POST /api/auth/*            │
└────────────┬────────────────────┘
             │
             ├─ NO ─┐
             │      │
    SÍ (Requiere JWT)
             │      │
             ▼      ▼
    ┌──────────────┐ ┌──────────────────┐
    │ verificar    │ │ Error 401        │
    │ Token        │ │ Unauthorized     │
    └──────┬───────┘ └──────────────────┘
           │
           ▼
    ┌──────────────┐
    │ Continuar    │
    │ Ejecución    │
    │ de Endpoint  │
    └──────────────┘
```

---

## 🎨 Componentes Frontend

### Navbar
```
┌─────────────────────────────────────────────────────────────┐
│ Logo │ Inicio│Servicios│Presupuesto│Galería│Testimonios│ ✕ │
│      │       │        │ 💰        │       │          │   │
│      │                                          [Iniciar Sesión]
│      │                                          O
│      │                                    [Perfil Usuario] [Logout]
└─────────────────────────────────────────────────────────────┘
```

### Modal de Autenticación
```
┌──────────────────────────────────────┐
│  ✕ Iniciar Sesión                    │
│                                      │
│  Elige tu forma de iniciar sesión    │
│                                      │
│  ┌──────────────────────────────────┐│
│  │ G  Continuar con Google          ││
│  └──────────────────────────────────┘│
│                                      │
│  ┌──────────────────────────────────┐│
│  │ f  Continuar con Facebook        ││
│  └──────────────────────────────────┘│
│                                      │
│          — o —                       │
│                                      │
│  Email: [___________________]        │
│  Password: [___________________]     │
│                                      │
│  ┌──────────────────────────────────┐│
│  │  Iniciar Sesión                  ││
│  └──────────────────────────────────┘│
│                                      │
│  ¿No tienes cuenta? Crear una       │
└──────────────────────────────────────┘
```

### Sección Presupuesto
```
┌──────────────────────────────┬────────────────────────┐
│  CONFIGURAR TU EVENTO        │  RESUMEN DE PRESUPUESTO│
│                              │                        │
│  Tipo Evento: [Dropdown ▼]   │  Tipo: Bodas           │
│  Personas: [50] [−] [+]      │  Personas: 50          │
│  Menú: ⦿ Básico              │  Menú: Premium         │
│         ○ Profesional         │  Precio/pp: $40        │
│         ○ Premium             │                        │
│                              │  ─────────────────────│
│  Servicios Adicionales:      │  Subtotal: $2000.00   │
│  ☐ Bartending ($300)          │  Servicios: $300.00    │
│  ☐ Meseros ($500)             │  ─────────────────────│
│  ☐ Decoración ($400)           │                        │
│  ☐ Postre ($200)               │  TOTAL: $2300.00      │
│                              │                        │
│  [Calcular Presupuesto]      │  [Solicitar Cotización]│
└──────────────────────────────┴────────────────────────┘
```

---

## 🔄 Ciclo de Vida de una Petición

```
1. Cliente hace petición HTTP
   ▼
2. Express recibe la petición
   ▼
3. Middleware procesa:
   - CORS Check
   - JSON Parse
   - JWT Verify (si aplica)
   ▼
4. Router encuentra endpoint
   ▼
5. Controlador procesa lógica
   ▼
6. Base de datos (si aplica)
   ▼
7. Respuesta JSON
   ▼
8. Cliente recibe datos
   ▼
9. JavaScript actualiza DOM
   ▼
10. Usuario ve cambios
```

---

## 🗄️ Modelo de Datos

### Usuario (Auth)
```javascript
{
  id: "google_abc123",
  name: "Juan Pérez",
  email: "juan@example.com",
  picture: "https://...",
  proveedor: "google",
  createdAt: "2024-11-06T10:30:00Z"
}
```

### Token JWT
```
Header: {
  "alg": "HS256",
  "typ": "JWT"
}

Payload: {
  "userId": "google_abc123",
  "email": "juan@example.com",
  "proveedor": "google",
  "iat": 1730858400,
  "exp": 1731463200
}

Signature: HMACSHA256(...)
```

### Contacto/Solicitud
```javascript
{
  id: "123",
  nombre: "María García",
  email: "maria@example.com",
  telefono: "+54 388 123-4567",
  fecha: "2024-12-25",
  servicio: "bodas",
  personas: 100,
  detalles: "Ceremonia religiosa",
  createdAt: "2024-11-06T10:30:00Z"
}
```

### Comentario/Testimonio
```javascript
{
  id: "456",
  nombre: "Carlos López",
  email: "carlos@example.com",
  texto: "Excelente servicio, muy profesionales",
  calificacion: 5,
  aprobado: true,
  createdAt: "2024-11-06T10:30:00Z"
}
```

---

## 🌐 Variables de Entorno

```
┌──────────────────────────────────────┐
│         .env Configuration           │
├──────────────────────────────────────┤
│                                      │
│ SERVIDOR                              │
│ • PORT=3000                          │
│ • NODE_ENV=development/production    │
│                                      │
│ EMPRESA                               │
│ • EMPRESA_NOMBRE                     │
│ • EMPRESA_TELEFONO                   │
│ • EMPRESA_EMAIL                      │
│ • EMPRESA_UBICACION                  │
│ • EMPRESA_WHATSAPP                   │
│                                      │
│ GOOGLE AUTH                           │
│ • GOOGLE_CLIENT_ID                   │
│ • GOOGLE_CLIENT_SECRET               │
│                                      │
│ FACEBOOK AUTH                         │
│ • FACEBOOK_APP_ID                    │
│ • FACEBOOK_APP_SECRET                │
│                                      │
│ SEGURIDAD                             │
│ • JWT_SECRET                         │
│                                      │
│ BASE DE DATOS                         │
│ • MONGODB_URI (opcional)             │
│                                      │
│ CONFIGURACIÓN                         │
│ • MAX_FILE_SIZE=50MB                │
│ • CORS_ORIGIN=*                      │
│                                      │
└──────────────────────────────────────┘
```

---

## 📊 Flujo de Datos - Presupuesto

```
Usuario Input
    │
    ├─ Tipo Evento
    ├─ Cantidad Personas
    ├─ Nivel Menú
    └─ Servicios Adicionales
    │
    ▼
JavaScript Calculation (presupuesto.js)
    │
    ├─ Validar datos
    ├─ Calcular subtotal menú
    ├─ Calcular servicios
    └─ Calcular total
    │
    ▼
sessionStorage
    │
    ├─ Guardar presupuesto
    ├─ Guardar opciones seleccionadas
    │
    ▼
Renderizar en UI
    │
    ├─ Actualizar summary panel
    ├─ Mostrar total con animación
    │
    ▼
Usuario ve resultado en tiempo real
```

---

## 🚀 Proceso de Deploy

```
┌─────────────────────────────────────┐
│     Desarrollo Local                 │
│  npm start → localhost:3000          │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│     Testing & QA                     │
│  • Testing manual en navegador       │
│  • Responsive test (mobile/desktop)  │
│  • API testing                       │
│  • Seguridad                         │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│     Preparación para Deploy          │
│  • .env actualizado                  │
│  • HTTPS configurado                 │
│  • BD conectada                      │
│  • Variables de entorno              │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│     Staging Environment              │
│  • Deploy a servidor de prueba       │
│  • Testing final                     │
│  • Verificar URLs externas           │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│     Producción                       │
│  • Deploy a servidor público         │
│  • Monitoring activo                 │
│  • Logging y backups                 │
│  • Soporte en vivo                   │
└─────────────────────────────────────┘
```

---

## 🔍 Monitoreo y Logging

```
┌─────────────────────────────────────┐
│     Application Monitoring           │
│                                      │
│ • Console Logs (desarrollo)          │
│ • Error Tracking                     │
│ • Request Logging                    │
│ • Performance Metrics                │
│ • User Analytics                     │
│ • Error Reporting                    │
│                                      │
└─────────────────────────────────────┘
```

---

## 🎯 Integración de Terceros

```
┌────────────────────────────────────────────┐
│          External Services                  │
├────────────────────────────────────────────┤
│                                            │
│ Google Identity Services (Autenticación)   │
│ • accounts.google.com/gsi/client           │
│                                            │
│ Facebook Graph API (Autenticación)         │
│ • connect.facebook.net/sdk.js              │
│                                            │
│ (Opcional en Producción)                   │
│                                            │
│ MongoDB Atlas (Base de Datos)              │
│ • mongodb+srv://cluster.mongodb.net        │
│                                            │
│ SendGrid / SMTP (Emails)                   │
│ • smtp.gmail.com                           │
│                                            │
│ Stripe/Mercado Pago (Pagos)                │
│ • api.stripe.com                           │
│                                            │
└────────────────────────────────────────────┘
```

---

**Diagrama actualizado:** Noviembre 2024
**Versión:** 2.0
**Mantenido por:** Equipo de Desarrollo
